[package]
name = "js-evaluator"
version = "0.2.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["rlib"]

[target.wasm32-wasip2.lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
boa_engine = { version = "0.19.0" }
wit-bindgen = "0.24.0"
